{
  "name": "servicenow-mcp-server",
  "version": "0.1.0",
  "description": "Mock ServiceNow ITSM as a stdio MCP server (incidents, requests, change, users, KB, CMDB).",
  "license": "MIT",
  "type": "module",
  "bin": {
    "servicenow-mcp-server": "dist/server.js"
  },
  "files": [
    "dist/",
    "src/data/"
  ],
  "scripts": {
    "build": "tsc && node -e \"require('fs').chmodSync('dist/server.js', 0o755)\" && cp -R src/data dist/data",
    "link": "npm link"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.4.0"
  }
}
