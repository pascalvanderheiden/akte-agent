{
  "name": "core-banking-mcp-server",
  "version": "0.1.0",
  "description": "Mock retail core-banking platform as a stdio MCP server (customers, accounts, transactions, cards, products, disputes, transfers).",
  "license": "MIT",
  "type": "module",
  "bin": {
    "core-banking-mcp-server": "dist/server.js"
  },
  "files": [
    "dist/",
    "src/data/"
  ],
  "scripts": {
    "build": "tsc && node -e \"require('fs').chmodSync('dist/server.js', 0o755)\" && cp -R src/data dist/data",
    "link": "npm link"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
