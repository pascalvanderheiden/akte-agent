# Fully pinned dependency lock for the OBO MCP server.
#
# Exact, mutually-compatible versions (resolved + install-verified) for
# reproducible container builds. The previous floating ">=" lower bounds let a
# rebuild silently pull untested/compromised upstream versions of
# security-critical libraries — notably PyJWT / cryptography, which perform the
# OBO access-token signature validation. Bump pins deliberately and re-freeze
# (ideally move to a hash-locked lockfile via pip-tools or uv).

# --- Direct dependencies ---
PyJWT[crypto]==2.13.0
azure-identity==1.25.3
httpx==0.28.1
mcp==1.28.1
starlette==1.3.1
uvicorn==0.52.0

# --- Transitive dependencies (pinned for reproducibility) ---
annotated-types==0.8.0
anyio==4.14.2
attrs==26.1.0
azure-core==1.41.0
certifi==2026.7.22
cffi==2.1.0
charset-normalizer==3.4.9
click==8.4.2
cryptography==49.0.0
h11==0.16.0
httpcore==1.0.9
httpx-sse==0.4.3
idna==3.18
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
msal==1.37.0
msal-extensions==1.3.1
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
python-dotenv==1.2.2
python-multipart==0.0.32
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
sse-starlette==3.4.6
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
